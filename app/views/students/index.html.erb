
<h1>Students index</h1>

<% @students.each do |student| %>
Student Id: <%= student.student_id %>, name: <%= student.name %>
<br>
<% marks = @marks.where(student_id: student.student_id)  %>
<% average = marks.map(&:mark).sum / marks.length %>
total Average: <%= average %>%
<br>
<% end  %>
